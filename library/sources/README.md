# Local source files

`private/` contains user-supplied source files copied into the local repository workspace. It is excluded from Git: the online repository contains edition metadata, hashes, citations and original learning artifacts, not the complete source books.

The 2026-09-05 task request authorizes analysis and a local copy of the supplied Bogle PDF. That request is recorded as the authority for this bounded research use; it is not a publisher redistribution license. Private database and extracted text remain outside version control.

The later all-Character collection remains in the owner's existing `Investing-Books/books` directory. The [current inventory](../catalog/INVENTORY_REPORT.md) references filenames under `TRADE_THEORIST_BOOKS_ROOT` and records their hashes; it does not copy the PDFs into Git. See the [library guide](../README.md) for verification and preparation.
The five added papers remain in the existing books directory and the two supplied OCR texts in its `transcripts/` subdirectory. Both text fingerprints and parent PDF fingerprints are registered. No transcripts or raw source text are published here.
